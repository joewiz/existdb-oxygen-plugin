# xst connection settings for local testing (copy to .env; .env is gitignored).
# xst walks up from the current dir to find .env, so this at the repo root covers
# any xst invocation under the repo. Defaults match the eXist Docker image.
EXISTDB_SERVER=http://localhost:8080
EXISTDB_USER=admin
EXISTDB_PASS=
